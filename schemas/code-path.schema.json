{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vibra.dev/schemas/code-path.schema.json",
  "title": "VibraCodePath",
  "description": "Canonical structural path. Strings are mapping keys; integers are sequence indices.",
  "type": "array",
  "items": {
    "oneOf": [
      { "type": "string" },
      { "type": "integer", "minimum": 0 }
    ]
  }
}
