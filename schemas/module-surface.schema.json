{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://vibra.dev/schemas/module-surface.schema.json",
  "title": "VibraModuleSurface",
  "description": "Loose schema for a parsed module root mapping (post-YAML). Keys are symbol names; values are arbitrary YAML-JSON encodings.",
  "type": "object",
  "propertyNames": {
    "pattern": "^-?[a-z][a-z0-9]*(-[a-z0-9]+)*$"
  },
  "additionalProperties": {
    "description": "Symbol definition: mapping, sequence, or scalar per Vibra spec"
  }
}
