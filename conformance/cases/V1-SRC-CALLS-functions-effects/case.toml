id = "V1-SRC-CALLS-functions-effects"
rule = "V1-SRC-CALLS"
profile = "static-v1"
operation = "type-check"
description = "Function values in Step 7 retain the empty-effect admission boundary."

[inputs]
source = "input.vib"

[expect]
accepted = false

[[expect.diagnostics]]
code = "@tool.unavailable"
level = "@error"
source = "input.vib"
span = [31, 42]
