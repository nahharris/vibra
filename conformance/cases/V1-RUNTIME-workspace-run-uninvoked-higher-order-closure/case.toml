id = "V1-RUNTIME-workspace-run-uninvoked-higher-order-closure"
rule = "V1-RUNTIME"
profile = "interpreter-v1"
operation = "workspace-run"
description = "An uninvoked closure with an indirect call does not affect the selected pure entry."

[inputs]
project = "tree/project.vibon"
tree = "tree"

[expect]
accepted = true
interpreter = { result = "result.vibon", audit_trace = "audit.vibon" }
