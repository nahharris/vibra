id = "V1-SRC-READER-source-loader-extension"
rule = "V1-SRC-READER"
profile = "reader-v1"
description = "The source loader rejects a data extension before parsing."

[inputs]
source = "input.vibon"

[expect]
accepted = false

[[expect.diagnostics]]
code = "@data.invalid-extension"
level = "@error"

[expect.diagnostics.span]
start = 0
end = 0
