id = "V1-PROJECT-workspace-check-binding-lambda-capture-shadow"
rule = "V1-PROJECT"
profile = "static-v1"
operation = "workspace-check"
description = "Workspace check relates a nested lambda parameter's shadowed outer binder, matching the single-source checker."

[inputs]
project = "tree/project.vibon"
tree = "tree"

[expect]
accepted = false

[[expect.diagnostics]]
code = "@name.redeclaration"
level = "@error"
source = "src/app/main.vib"
span = [88, 93]

  [[expect.diagnostics.related]]
  source = "src/app/main.vib"
  span = [13, 18]
