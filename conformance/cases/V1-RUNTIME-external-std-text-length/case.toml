id = "V1-RUNTIME-external-std-text-length"
rule = "V1-RUNTIME"
profile = "interpreter-v1"
operation = "interpret"
description = "The verified explicit @std.text import counts Unicode scalars after concatenation."

[inputs]
source = "input.vib"

[expect]
accepted = true
interpreter = { result = "result.vibon", audit_trace = "audit.vibon" }
