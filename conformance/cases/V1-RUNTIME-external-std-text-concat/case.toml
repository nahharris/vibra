id = "V1-RUNTIME-external-std-text-concat"
rule = "V1-RUNTIME"
profile = "interpreter-v1"
operation = "interpret"
description = "The verified explicit @std.text import executes Unicode concatenation with an empty operand."

[inputs]
source = "input.vib"

[expect]
accepted = true
interpreter = { result = "result.vibon", audit_trace = "audit.vibon" }
