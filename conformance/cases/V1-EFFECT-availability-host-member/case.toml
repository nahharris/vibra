id = "V1-EFFECT-availability-host-member"
rule = "V1-EFFECT"
profile = "static-v1"
operation = "type-check"
description = "A valid effect member using the M4 @host provider is unavailable in M2."

[inputs]
source = "input.vib"

[expect]
accepted = false

[[expect.diagnostics]]
code = "@tool.unavailable"
level = "@error"
source = "input.vib"
span = [0, 97]
