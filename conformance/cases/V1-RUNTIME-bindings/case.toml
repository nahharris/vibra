id = "V1-RUNTIME-bindings"
rule = "V1-RUNTIME"
profile = "interpreter-v1"
operation = "interpret"
description = "Immutable module values, local let bindings, sequencing, boolean if, and fixed calls execute through checked IR."

[inputs]
source = "input.vib"

[expect]
accepted = true
interpreter = { result = "result.vibon", audit_trace = "audit.vibon" }
