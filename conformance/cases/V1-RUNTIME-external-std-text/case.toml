id = "V1-RUNTIME-external-std-text"
rule = "V1-RUNTIME"
profile = "interpreter-v1"
operation = "interpret"
description = "The verified explicit @std.text import reaches both closed compiler intrinsics."

[inputs]
source = "input.vib"

[expect]
accepted = true
interpreter = { result = "result.vibon", audit_trace = "audit.txt" }
