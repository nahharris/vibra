id = "V1-PROJECT-resolve-entry-private"
rule = "V1-PROJECT-resolution"
profile = "static-v1"
operation = "resolve"
description = "The privileged entry slot may name a private function in its own target."

[inputs]
project = "tree/project.vibon"
tree = "tree"

[expect]
accepted = true
resolved = "resolved.vibon"
