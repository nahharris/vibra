id = "V1-RUNTIME-tail-mutual"
rule = "V1-RUNTIME"
profile = "interpreter-v1"
operation = "interpret"
description = "Direct and mutual tail calls reuse one activation and preserve the returned value."

[inputs]
source = "input.vib"

[expect]
accepted = true
interpreter = { result = "result.vibon", audit_trace = "audit.vibon" }
